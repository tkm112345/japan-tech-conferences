# セキュリティポリシー

## このリポジトリの性質

これは **データのみ** のリポジトリです。配布物は JSON データであり、
利用者のブラウザやサーバーで自動実行されるコードは含みません。

## 貢献を受け入れる際の対策

不特定多数からの Pull Request を前提に、次の設計にしています。

- **URL は https のみ許可**（`schema.json` の `pattern` で強制）。`javascript:` や `http:` は弾かれます。
- **CI は `pull_request` トリガー**を使用。`pull_request_target` は使いません。
  これによりフォークからの変更にリポジトリのシークレットや書き込み権限が渡りません。
- **ワークフロー権限は `contents: read` のみ**（最小権限）。
- 依存関係は検証用の `ajv` のみ。`npm ci` は lockfile 固定でインストールします。

## データを利用する側への注意

`name` や `city` などの文字列はコミュニティ入力です。Web ページ等で表示する際は、
**必ずエスケープ**してから描画してください（XSS 対策）。`url` は https のみですが、
リンクを開く前にドメインを確認することを推奨します。

## 脆弱性の報告

問題を見つけた場合は、公開 Issue ではなく GitHub の
[Security advisories](../../security/advisories/new) から非公開で報告してください。
